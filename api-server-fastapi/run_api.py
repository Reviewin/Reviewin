#import os
#os.system("uvicorn api:api --reload")
import requests
import couchdb 
liste_email = ['Billel', 'soundous', 'ayoub', 'soundouse', 'tarek']
response = requests.get(f"http://")
#création d'email full inscriptions
